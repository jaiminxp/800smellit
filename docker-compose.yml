version: '3'
services:
  api:
    container_name: 800smellit-api
    build: .
    ports:
      - '3000:3000'
    volumes:
      - ./apps:/app/apps:ro
      - /app/node_modules
    env_file:
      - apps/api/.env
